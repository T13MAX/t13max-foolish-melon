<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.t13max.melon-plugin</groupId>
    <artifactId>nms-foolish-melon-island</artifactId>
    <version>1.0.1</version>
    <packaging>jar</packaging>

    <name>nms-foolish-melon-island</name>
    <url>https://gitee.com/t13max</url>

    <properties>
        <java.version>17</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <melon.island.version>1.0.1</melon.island.version>
        <melon.nms.version>1.0.1</melon.nms.version>
        <t13max-ai.version>1.0.1</t13max-ai.version>
        <nms.version>1.20.1-SNAPSHOT</nms.version>
    </properties>
    <pluginRepositories>
        <pluginRepository>
            <id>bytecode.space</id>
            <url>https://repo.bytecode.space/repository/maven-public/</url>
        </pluginRepository>
    </pluginRepositories>
    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.8.1</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                </configuration>
            </plugin>
            <plugin>
                <groupId>ca.bkaw</groupId>
                <artifactId>paper-nms-maven-plugin</artifactId>
                <version>1.4.4</version>
                <executions>
                    <execution>
                        <phase>process-classes</phase>
                        <goals>
                            <goal>remap</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
            </resource>
        </resources>
    </build>
    <dependencies>
        <dependency>
            <groupId>com.t13max.melon-plugin</groupId>
            <artifactId>common-plugin</artifactId>
            <version>${melon.island.version}</version>
        </dependency>
        <dependency>
            <groupId>com.t13max.common</groupId>
            <artifactId>t13max-ai</artifactId>
            <version>${t13max-ai.version}</version>
        </dependency>

        <dependency>
            <groupId>ca.bkaw</groupId>
            <artifactId>paper-nms</artifactId>
            <version>${nms.version}</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>
</project>
